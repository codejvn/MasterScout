import { REMOVE_FROM_PICKLIST } from '../Actions/removeFromPicklist';


const picklistReducer = ( state = teams, action = {}) => {
    switch(action.type) {
        case REMOVE_FROM_PICKLIST:            
    }
}